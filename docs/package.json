{
  "name": "neonjsx-lazy-example",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.tsx --bundle --format=esm --platform=browser --jsx=transform --jsx-factory=h --jsx-fragment=Fragment --outdir=public/_default --splitting --entry-names=[name]",
    "serve": "npx serve public"
  },
  "dependencies": {
    "@nutsloop/neonjsx": "^1.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.2",
    "serve": "^14.2.0"
  }
}
